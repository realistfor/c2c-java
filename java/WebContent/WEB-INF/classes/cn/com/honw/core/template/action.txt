package #{action_p};

import javax.servlet.http.HttpServletRequest;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.context.request.WebRequest;
/**
 * 项目名：社区娱乐项目
 * 包名：org.clover.onetime.service.#{Entity}
 * 文件名：#{Entity}Controller.java
 * 版本信息：version V0.5
 * 日期：2012/04/09-09:06:04
 * Copyright (c) 2012一点私家公司-版权所有
 */

@Controller
public class #{Entity}Controller {
	private static final Logger log = LoggerFactory.getLogger(#{Entity}Controller.class);

	@RequestMapping(value={"/#{config_entity}"})
	public String list(@PathVariable String uniqueId, 
			@PathVariable  String mode, WebRequest webRequest) {
//		webRequest.setAttribute(Constants.PROJECT_UNIQUE_KEY_READABLE, 
//				uniqueId, WebRequest.SCOPE_REQUEST);
//		webRequest.setAttribute(Constants.FEATURE_CODE_KEY_READABLE, 
//				ProjectMenus.FEATURE_CALENDAR, WebRequest.SCOPE_REQUEST);
		return "page:#{config_entity}/list";
	}

}